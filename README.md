# Rust API using Rocket

This is a Rocket json HTTP API where we can start your own API.

## Running

1. Clone this repo

2. Install rust nightly version

    ``` 
   rustup toolchain install nightly
   ``` 
3. 
    ``` 
    cargo run
    ``` 

